# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
font pango:Source Code Pro 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id urxvtc

#Function keys
#clipboard
bindsym $mod+F1 exec --no-startup-id /home/dyeprii/bin/scripts/clipmenu  -y 500 -x 640 -h 32 -w 640 -i -p "" -sf "#ededed" -nf "#ededed" -nb "#1e1e1e" -sb "#1e1e1e" -fn "Source Code Pro Light-10" -sb "#0088CC"
#switcher
#bindsym $mod+Tab exec --no-startup-id /home/dyeprii/bin/scripts/winmenu.py -y 500 -x 640 -h 32 -w 640 -i -q -p "$" -sf "#ededed" -nf "#ededed" -nb "#1e1e1e" -sb "#1e1e1e" -fn "Source Code Pro-9:style=Bold" -sb "#0088CC"
bindsym $mod+Tab exec --no-startup-id dmenu-i3-window-jumper

# toggle border
bindsym $mod+t border toggle
# screenshot
bindsym Print exec --no-startup-id scrot ~/Pictures/"Screenshot from $(date).png"
bindsym $mod+Print exec --no-startup-id scrot -u ~/Pictures/"Screenshot from $(date).png"

# kill focused window
bindsym $mod+Shift+q kill

#kill with middle button
bindsym $mod+button2 kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run  -y 500 -x 640 -h 32 -w 640 -i -q -p "" -sf "#d5d5d5" -nf "#d5d5d5" -nb "#1e1e1e" -sb "#1e1e1e" -fn "Source Code Pro Light-10" -sb "#0088CC"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# default layout
#workspace_layout split

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# quake-like term hack
exec --no-startup-id urxvtc -name metask
for_window [instance="metask"] floating enable;
for_window [instance="metask"] move scratchpad; [instance="metask"] scratchpad show; move position 0px 0px; resize set 1920px 350px; move scratchpad
bindsym $mod+grave [instance="metask"] scratchpad show

# toogle tray_output
#bindsym button1 bar mode toggle

#home
set $w1 1:
#term
set $w2 2:
#mail
set $w3 3:
#remote
set $w4 4:
#media
set $w5 5:
#games
set $w6 6:
#etc
set $w7 7:
#chat
set $w8 8:
set $w9 9
set $w10 10:

# automatically move app to workspace
assign [class="Firefox"] $w1
assign [class="Chromium"] $w1
assign [class="URxvt"] $w2
assign [class="Vncviewer"] $w4
assign [class="quasselclient"] $w3
assign [class="Thunderbird"] $w3
assign [class="Claws-mail"] $w3
assign [class="Skype"] $w3
assign [class="Zoiper"] $w3
assign [class="quassel"] $w3
assign [class="remmina"] $w4
assign [class="Remmina"] $w4
assign [class="Spotify"] $w5
assign [class="Gimp"] $w5
assign [class="Steam"] $w6
assign [class="Hexchat"] $w8
assign [class="hexchat"] $w8
assign [class="Dwarf_Fortress"] $w6
assign [class="hl2_linux"] $w10
assign [title="glances"] $w10

# floating windows
for_window [title="Remmina Remote Desktop Client"] floating enable
for_window [class="Vlc"] floating enable
for_window [class="Whatpulse"] floating enable
for_window [class="Filezilla"] floating enable
for_window [class="Xarchiver"] floating enable
for_window [class="Xfd"] floating enable
for_window [class="Lxappearance"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Organizer"] floating enable
for_window [title="Preferences$"] floating enable
for_window [class="Transmission-gtk"] floating enable
#for_window [class="Thunar"] floating enable
for_window [class="Gpicview"] floating enable
for_window [class="Zoiper"] floating enable
for_window [class="Firefox" instance="Browser"] floating enable
for_window [class="Galculator"] floating enable
for_window [class="hl2_linux"] floating enable
for_window [class="Gedit"] floating enable
for_window [class="KeePass2"] floating enable
for_window [class="feh"] floating enable
for_window [class="Gucharmap"] floating enable
for_window [class="Skype"] floating enable
for_window [class="Skype"] floating enable
#for_window [class="Firefox"] floating enable
for_window [instance="rxvt-app-launcher"] floating enable
for_window [title="Open Database File"] floating enable
for_window [title="Create New Password Database"] floating enable
for_window [title="Select key file manually"] floating enable
for_window [instance="xpad"] sticky enable
for_window [class="xpad"] floating enable
for_window [class="keepassx"] floating enable

# move to center
bindsym $mod+c move absolute position center

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace $w1
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8
bindsym $mod+9 workspace $w9
bindsym $mod+0 workspace $w10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+8 move container to workspace $w8
bindsym $mod+Shift+9 move container to workspace $w9
bindsym $mod+Shift+0 move container to workspace $w10

# auto move back and forth
workspace_auto_back_and_forth yes
bindsym $mod+z workspace next
bindsym $mod+Shift+z workspace prev

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# set locker to be used below
set $Locker /home/dyeprii/bin/scripts/lock_wrapper.sh && sleep 1

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Pause mode "$mode_system"


# resize window (you can also use the mouse for that)
mode "  " {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "  "

#move to urgent
bindsym $mod+x [urgent=latest] focus

#scratchpad
# Make the currently focused window a scratchpad
bindsym $mod+F4 move scratchpad
#
# Show the first scratchpad window
bindsym $mod+F3 scratchpad show

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
  mode hide
  hidden_state hide
  tray_output none
  modifier Mod3
  status_command i3blocks -c ~/.config/i3blocks.conf
  font pango:Source Code Pro Light, FontAwesome 9
  #font pango:monofur for Powerline Bold 10
  #font pango:Source Code Pro Bold 9
  position top
  #height 20
  #tray_output none
  colors {
    separator #666666
    background #222222
    statusline #d9d9d9
    focused_workspace #0088CC #0088CC #ffffff
    active_workspace #222222 #222222 #ffffff
    inactive_workspace #222222 #222222 #888888
    urgent_workspace #900000 #900000 #ffffff
  }
}

bar {
  status_command i3blocks -c ~/.config/i3blocks.conf
  font pango:Source Code Pro Light, FontAwesome 9
  #font pango:monofur for Powerline Bold 10
  #font pango:Source Code Pro Bold 9
  position top
  #height 20
  tray_output HDMI-1
  modifier Mod3
  colors {
    separator #666666
    background #222222
    statusline #d9d9d9
    focused_workspace #0088CC #0088CC #ffffff
    active_workspace #222222 #222222 #ffffff
    inactive_workspace #222222 #222222 #888888
    urgent_workspace #900000 #900000 #ffffff
  }
}

client.focused #0088CC #0088CC #ffffff #d9d9d9
client.focused_inactive #333333 #333333 #888888 #333333
client.unfocused #333333 #333333 #888888 #333333
client.urgent #900000 #900000 #ffffff #900000

# my gaps

for_window [class="^.*"] border pixel 2
for_window [title="^.*"] border pixel 2
gaps inner 10
gaps outer 5

#autostart applications
exec --no-startup-id firefox
exec --no-startup-id /home/dyeprii/bin/scripts/wallpaper.sh
exec --no-startup-id compton -b -m1 --config ~/.compton.conf
exec --no-startup-id xautolock -time 10 -notify 10 -notifier "notify-send -u critical -t 100 -- 'LOCKING screen in 10 seconds'" -locker /home/dyeprii/bin/scripts/lock_wrapper.sh
exec --no-startup-id /home/dyeprii/bin/scripts/clipmenud
#exec --no-startup-id xpad
exec --no-startup-id /usr/bin/redshift -l 14.554729000000002:121.02444519999995
#exec --no-startup-id thunar --daemon
#exec --no-startup-id dunst
#exec --no-startup-id /usr/bin/claws-mail
#exec --no-startup-id /usr/bin/hexchat

